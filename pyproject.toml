[project]
name = "peatland-rewetting-utac"
version = "1.0.0"
description = "Peatland rewetting: Guenther 2020 CO2 sink + CH4 rise, Moorschutzstrategie DE 2022, Kreyling 2021 biodiversity honesty -- no UTAC/CREP/AFET bridge"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Römer, Johann" }]
requires-python = ">=3.11"
dependencies = []

[project.urls]
Homepage = "https://github.com/GenesisAeon/peatland-rewetting-utac"
Repository = "https://github.com/GenesisAeon/peatland-rewetting-utac"

[project.optional-dependencies]
dev = [
    "ruff>=0.6.0",
    "mypy>=1.10.0",
    "pre-commit>=3.7.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/peatland_rewetting_utac"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "W", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=peatland_rewetting_utac --cov-report=term-missing -v"
